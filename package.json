{
  "name": "pwanote",
  "version": "1.0.0",
  "description": "TopLearn PWA Course Project",
  "main": "main.js",
  "scripts": {
    "start": "http-server",
    "start:ssl": "http-server -c-1 -S -C cert.pem -K key.pem",
    "test": "echo \"Error: no test specified\" && exit 1",
    "json-server": "json-server --watch ./utils/db.json --port 6060"
  },
  "keywords": [
    "PWA",
    "TopLearn"
  ],
  "author": "TopLearn.com",
  "license": "MIT",
  "dependencies": {
    "chalk": "^5.1.2"
  },
  "devDependencies": {
    "http-server": "^0.10.0"
  }
}
